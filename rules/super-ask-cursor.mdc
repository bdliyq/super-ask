---
description: "super-ask（Cursor）已改为 hook 驱动"
alwaysApply: true
---
# super-ask（Cursor Hook 模式）

## 适用范围

仅适用于 Cursor 主 agent。

## 当前机制

Cursor 版 super-ask 不再依赖“每轮结束前手动执行 CLI”的 rules 约束，而是由 hook 自动驱动：

1. `.cursor/hooks.json` 会注册全部 Cursor hook 事件到 `node "{{SUPER_ASK_CURSOR_CLI}}" --cursor-hook`
2. 只有 `stop` 事件会真正调用 super-ask；其他事件只打印日志
3. `stop` hook 会读取 transcript，提取本轮最后一条 assistant 文本，自动发送给 super-ask
4. super-ask 返回的用户反馈会通过 `followup_message` 自动续接到 Cursor 对话

## 你需要遵守的事项

1. 不要再把“每轮结束前手动执行 `node "{{SUPER_ASK_CLI}}"`”当作常规动作，`stop` hook 会自动完成
2. 不要在 Cursor 里维护额外的 super-ask 会话环境变量，也不要手动拼 `--session-id` 做常规汇报
3. 结束当前回合前，最后一条 assistant 消息要足够完整，因为它会被 `stop` hook 当作汇报摘要
4. 如果用户明确要求“中途先问我一下”“手动调试 super-ask / hook / server”，才手动运行 CLI

## 手动调试入口（仅在明确需要时）

```bash
node "{{SUPER_ASK_CLI}}" \
  --summary '## 工作汇报
- 已完成 xxx
- 结果：xxx' \
  --question '请确认是否继续？' \
  --title '任务标题' \
  --session-id '<可选 chatSessionId>' \
  --source cursor \
  --workspace-root '<当前工作区的绝对路径>' \
  --options '验收通过' '需要修改' \
  --retries -1 \
  --port 19960
```

## 注意事项

- 禁止使用 `curl` / `wget` 直接调用 super-ask API
- 手动调试时，仍然只能通过 `node "{{SUPER_ASK_CLI}}"` 调用
- 如果 super-ask server 未启动，可运行 `bash "{{SUPER_ASK_INSTALL_SH}}"`
- hook 脚本路径为 `{{SUPER_ASK_CURSOR_CLI}}`
